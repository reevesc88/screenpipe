interface:
  display_name: "Screenpipe"
  short_description: "Repo-specific patterns and workflows for screenpipe"
  default_prompt: "Use the screenpipe repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true